openapi: 3.0.3
info:
  title: HH Education API
  version: 0.1.0
  description: API documentation for the current backend implementation.
servers:
  - url: http://localhost:3000
    description: Local development server

tags:
  - name: Auth
    description: Authentication and token lifecycle
  - name: Users
    description: User profile and user management
  - name: Courses
    description: Course catalog and course management

paths:
  /auth/register:
    post:
      tags:
        - Auth
      summary: Register student account
      description: Register a new student account. Email verification is currently bypassed and the account is created as active.
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
            example:
              fullName: Nguyen Van A
              email: student@example.com
              password: Password123!
              confirmPassword: Password123!
      responses:
        '201':
          description: Student account created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Email already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                error:
                  code: EMAIL_ALREADY_EXISTS
                  message: Email already exists. Please use a different email address.
                  details: null

  /auth/login:
    post:
      tags:
        - Auth
      summary: Login
      description: Authenticate credentials and create a login session with access and refresh tokens.
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
            example:
              email: student@example.com
              password: Password123!
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                error:
                  code: INVALID_CREDENTIALS
                  message: Invalid credentials. Please check your email and password and try again.
                  details: null
        '403':
          description: Account is banned or not verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/refresh-token:
    post:
      tags:
        - Auth
      summary: Refresh token
      description: Rotate refresh token and issue a new access token and refresh token.
      operationId: refreshToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RefreshTokenRequest'
            example:
              refreshToken: jwt-refresh-token
      responses:
        '200':
          description: Token refreshed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefreshTokenSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid refresh token or refresh token reused
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                invalidRefreshToken:
                  value:
                    success: false
                    error:
                      code: INVALID_REFRESH_TOKEN
                      message: Invalid refresh token. Please log in again.
                      details: null
                refreshTokenReused:
                  value:
                    success: false
                    error:
                      code: REFRESH_TOKEN_REUSED
                      message: Refresh token reuse detected. Please log in again.
                      details: null
        '403':
          description: Account is banned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/me:
    get:
      tags:
        - Users
      summary: Get current user profile
      description: Return the profile of the authenticated user.
      operationId: getMe
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current user profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetMeSuccessResponse'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                unauthorized:
                  value:
                    success: false
                    error:
                      code: UNAUTHORIZED
                      message: Unauthorized. Please log in to access this resource.
                      details: null
                accessTokenExpired:
                  value:
                    success: false
                    error:
                      code: ACCESS_TOKEN_EXPIRED
                      message: Access token expired. Please refresh your session.
                      details: null
                invalidAccessToken:
                  value:
                    success: false
                    error:
                      code: INVALID_ACCESS_TOKEN
                      message: Invalid access token. Please log in again.
                      details: null
        '403':
          description: Account is banned or not verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User profile not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/teachers:
    post:
      tags:
        - Users
      summary: Create teacher account
      description: Admin creates an active teacher account. Teacher email verification is bypassed for now.
      operationId: createTeacher
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeacherRequest'
            example:
              fullName: Teacher A
              email: teacher@example.com
              password: Password123!
              confirmPassword: Password123!
      responses:
        '201':
          description: Teacher created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateTeacherSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Email already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/users:
    get:
      tags:
        - Users
      summary: List users
      description: Admin lists users with offset pagination, optional role/status filters, and keyword search.
      operationId: listUsers
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
          description: Page number. Offset is calculated as `(page - 1) * limit`.
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          description: Number of users per page.
        - in: query
          name: role
          schema:
            $ref: '#/components/schemas/UserRole'
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/UserStatus'
        - in: query
          name: keyword
          schema:
            type: string
            minLength: 1
            maxLength: 100
          description: Search by email or full name.
      responses:
        '200':
          description: User list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListUsersSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/courses:
    get:
      tags:
        - Courses
      summary: List admin courses
      description: Admin lists non-deleted courses with pagination, optional status/teacher filters, and keyword search.
      operationId: listAdminCourses
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/CourseStatus'
        - in: query
          name: teacherId
          schema:
            type: string
            format: uuid
        - in: query
          name: search
          schema:
            type: string
            minLength: 1
            maxLength: 100
      responses:
        '200':
          description: Course list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAdminCoursesSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    post:
      tags:
        - Courses
      summary: Create course draft
      description: Admin creates a draft course and assigns it to an active teacher. Slug is generated by server from title plus a short timestamp suffix.
      operationId: createCourse
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCourseRequest'
            example:
              title: Lap trinh C co ban
              description: Khoa hoc nhap mon lap trinh C
              teacherId: 00000000-0000-0000-0000-000000000000
              price: 500000
              salePrice: 399000
              allowPreview: true
      responses:
        '201':
          description: Course draft created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCourseSuccessResponse'
        '400':
          description: Validation failed or teacher is invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                validationFailed:
                  value:
                    success: false
                    error:
                      code: BAD_REQUEST
                      message: Validation failed
                      details: []
                invalidTeacher:
                  value:
                    success: false
                    error:
                      code: INVALID_COURSE_TEACHER
                      message: Teacher does not exist, is not active, or is not a teacher account.
                      details: null
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Generated course slug already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: false
                error:
                  code: COURSE_SLUG_ALREADY_EXISTS
                  message: Course slug already exists. Please use a different slug.
                  details: null

  /admin/courses/{courseId}:
    get:
      tags:
        - Courses
      summary: Get admin course detail
      description: Admin gets one non-deleted course by id.
      operationId: getAdminCourse
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/CourseIdPath'
      responses:
        '200':
          description: Course detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCourseSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Course not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    patch:
      tags:
        - Courses
      summary: Update course metadata
      description: Admin updates course metadata. Thumbnail URL is updated here after the future upload flow returns a URL.
      operationId: updateCourse
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/CourseIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCourseRequest'
            example:
              title: Lap trinh C co ban update
              description: Mo ta moi
              thumbnailUrl: https://cdn.example.com/courses/c-course.jpg
              price: 500000
              salePrice: 399000
              allowPreview: true
      responses:
        '200':
          description: Course updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCourseSuccessResponse'
        '400':
          description: Validation failed, teacher is invalid, or course status cannot be edited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Course not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/courses/{courseId}/publish:
    patch:
      tags:
        - Courses
      summary: Publish course
      description: Admin publishes a draft course. Published courses are returned unchanged.
      operationId: publishCourse
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/CourseIdPath'
      responses:
        '200':
          description: Course published
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCourseSuccessResponse'
        '400':
          description: Course status does not allow publishing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Course not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /admin/courses/{courseId}/archive:
    patch:
      tags:
        - Courses
      summary: Archive course
      description: Admin archives a course. Archived courses are returned unchanged.
      operationId: archiveCourse
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/CourseIdPath'
      responses:
        '200':
          description: Course archived
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCourseSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Missing, expired, or invalid access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Course not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /catalog/courses:
    get:
      tags:
        - Courses
      summary: List published courses
      description: Public catalog endpoint. Only published, non-deleted courses are returned.
      operationId: listCatalogCourses
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - in: query
          name: search
          schema:
            type: string
            minLength: 1
            maxLength: 100
      responses:
        '200':
          description: Published course list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListCatalogCoursesSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'

  /catalog/courses/{courseSlug}:
    get:
      tags:
        - Courses
      summary: Get published course detail
      description: Public course detail by slug, including ordered chapters and lessons.
      operationId: getCatalogCourse
      parameters:
        - in: path
          name: courseSlug
          required: true
          schema:
            type: string
            minLength: 2
            maxLength: 255
      responses:
        '200':
          description: Published course detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatalogCourseSuccessResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: Course not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  parameters:
    CourseIdPath:
      in: path
      name: courseId
      required: true
      schema:
        type: string
        format: uuid

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    BadRequest:
      description: Validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            success: false
            error:
              code: BAD_REQUEST
              message: Validation failed
              details:
                - location: body
                  field: email
                  message: Invalid email

  schemas:
    UserRole:
      type: string
      enum:
        - admin
        - teacher
        - student

    UserStatus:
      type: string
      enum:
        - pending_verification
        - active
        - banned

    CourseStatus:
      type: string
      enum:
        - draft
        - published
        - archived

    RegisterRequest:
      type: object
      required:
        - fullName
        - email
        - password
        - confirmPassword
      additionalProperties: false
      properties:
        fullName:
          type: string
          minLength: 2
          maxLength: 100
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
          maxLength: 64
          description: Must contain uppercase, lowercase, number, and special character.
        confirmPassword:
          type: string
          minLength: 8
          maxLength: 64

    RegisterResponseData:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        fullName:
          type: string
        role:
          type: string
          enum:
            - student
        status:
          type: string
          enum:
            - active

    RegisterSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/RegisterResponseData'

    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
          maxLength: 64

    AuthUser:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        fullName:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        status:
          $ref: '#/components/schemas/UserStatus'

    LoginResponseData:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        user:
          $ref: '#/components/schemas/AuthUser'

    LoginSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/LoginResponseData'

    RefreshTokenRequest:
      type: object
      required:
        - refreshToken
      additionalProperties: false
      properties:
        refreshToken:
          type: string

    RefreshTokenResponseData:
      type: object
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string

    RefreshTokenSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/RefreshTokenResponseData'

    GetMeResponseData:
      allOf:
        - $ref: '#/components/schemas/AuthUser'
        - type: object
          properties:
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time

    GetMeSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/GetMeResponseData'

    CreateTeacherRequest:
      allOf:
        - $ref: '#/components/schemas/RegisterRequest'

    CreateTeacherResponseData:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        fullName:
          type: string
        role:
          type: string
          enum:
            - teacher
        status:
          type: string
          enum:
            - active

    CreateTeacherSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/CreateTeacherResponseData'

    AdminUserItem:
      allOf:
        - $ref: '#/components/schemas/AuthUser'
        - type: object
          properties:
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time

    OffsetPagination:
      type: object
      properties:
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 20
        totalItems:
          type: integer
          example: 1015
        totalPages:
          type: integer
          example: 51

    ListUsersResponseData:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminUserItem'
        pagination:
          $ref: '#/components/schemas/OffsetPagination'

    ListUsersSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/ListUsersResponseData'

    CreateCourseRequest:
      type: object
      required:
        - title
        - teacherId
        - price
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 2
          maxLength: 255
        description:
          type: string
          maxLength: 5000
        teacherId:
          type: string
          format: uuid
        price:
          type: integer
          format: int64
          minimum: 0
          maximum: 9007199254740991
        salePrice:
          type: integer
          format: int64
          nullable: true
          minimum: 0
          maximum: 9007199254740991
          description: Must be less than price when provided.
        allowPreview:
          type: boolean
          default: true

    UpdateCourseRequest:
      type: object
      minProperties: 1
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 2
          maxLength: 255
        description:
          type: string
          nullable: true
          maxLength: 5000
        teacherId:
          type: string
          format: uuid
        thumbnailUrl:
          type: string
          format: uri
          nullable: true
        price:
          type: integer
          format: int64
          minimum: 0
          maximum: 9007199254740991
        salePrice:
          type: integer
          format: int64
          nullable: true
          minimum: 0
          maximum: 9007199254740991
          description: Must be less than price when provided.
        allowPreview:
          type: boolean

    AdminCourseTeacher:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        fullName:
          type: string

    AdminCourseResponseData:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
        teacherId:
          type: string
          format: uuid
        teacher:
          $ref: '#/components/schemas/AdminCourseTeacher'
        thumbnailUrl:
          type: string
          nullable: true
        price:
          type: integer
          format: int64
        salePrice:
          type: integer
          format: int64
          nullable: true
        status:
          $ref: '#/components/schemas/CourseStatus'
        allowPreview:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateCourseSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/AdminCourseResponseData'

    ListAdminCoursesResponseData:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminCourseResponseData'
        pagination:
          $ref: '#/components/schemas/OffsetPagination'

    ListAdminCoursesSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/ListAdminCoursesResponseData'

    CatalogCourseTeacher:
      type: object
      properties:
        id:
          type: string
          format: uuid
        fullName:
          type: string

    CatalogCourseItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
        teacher:
          $ref: '#/components/schemas/CatalogCourseTeacher'
        thumbnailUrl:
          type: string
          nullable: true
        price:
          type: integer
          format: int64
        salePrice:
          type: integer
          format: int64
          nullable: true
        status:
          $ref: '#/components/schemas/CourseStatus'
        allowPreview:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ListCatalogCoursesResponseData:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CatalogCourseItem'
        pagination:
          $ref: '#/components/schemas/OffsetPagination'

    ListCatalogCoursesSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/ListCatalogCoursesResponseData'

    CatalogLesson:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        type:
          type: string
          enum:
            - video
            - text
            - quiz
        durationSec:
          type: integer
          nullable: true
        orderIndex:
          type: integer

    CatalogChapter:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        orderIndex:
          type: integer
        lessons:
          type: array
          items:
            $ref: '#/components/schemas/CatalogLesson'

    CatalogCourseResponseData:
      allOf:
        - $ref: '#/components/schemas/CatalogCourseItem'
        - type: object
          properties:
            chapters:
              type: array
              items:
                $ref: '#/components/schemas/CatalogChapter'

    CatalogCourseSuccessResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/CatalogCourseResponseData'

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              nullable: true
